interface:
  display_name: "Harnest Authoring"
  short_description: "Build and modify Harnest agent projects"
  default_prompt: "Use $harnest-authoring to modify this Harnest agent project correctly and validate the result."
policy:
  allow_implicit_invocation: true
