# yaml-language-server: $schema=../../schemas/config.schema.json
apiVersion: harnest.dev/v1alpha1
kind: Agent
metadata:
  name: jev-triage
  displayName: Jev Support Triage
spec:
  entrypoint: agent:root_agent
  framework:
    name: adk
    mode: managed
  runtime:
    version: "3.12"
    dependencyFile: pyproject.toml
server:
  http:
    port: 1907
